{% extends 'base.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
    <h1>Create new Product</h1>

{#    <div class="alert alert-danger">#}
{#        # twig if.#}
{#        {% for error in errors %}#}
{#            <li>{{ error }}</li>#}
{#        {% endfor %}#}
{#    </div>#}
    {{ include('main/product/_form.html.twig') }}

    <a href="{{ path('product_index') }}">back to list</a>
{% endblock %}
