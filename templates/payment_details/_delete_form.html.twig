<form method="post" action="{{ path('payment_details_delete', {'id': payment_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_detail.id) }}">
    <button class="btn">Delete</button>
</form>
